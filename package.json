{
  "name": "auth0-rate-limiter",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "chai-http": "^4.3.0",
    "dotenv": "^16.0.1",
    "http-server": "^14.0.0",
    "mocha": "^9.2.2",
    "nodemon": "^2.0.18"
  },
  "scripts": {
    "start": "nodemon ./src/main.js",
    "test": "mocha ./specs/**/*.js",
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "prettier": "2.7.1"
  }
}
